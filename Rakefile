require 'bundler'
Bundler.require

require 'sinatra/activerecord/rake'

ActiveRecord::Base.establish_connection(
  :host => '',
  :adapter  => "postgresql",
  :database => "games_db"
)

namespace :db do

end